#include "pch.h"
#include "harness.h"


int day11(const stringlist& input)
{
    for (auto& line : input)
    {
        (void)line;
    }

    return -1;
}

int day11_2(const stringlist& input)
{
    for (auto& line : input)
    {
        (void)line;
    }

    return -1;
}


void run_day11()
{
    string sample =
        R"()";

    test(-100, day11(READ(sample)));
    skip("cos i haven't done it yet");
    skip("cos i haven't done it yet");
    //gogogo(day11(LOAD(11)));

    //test(-100, day11_2(READ(sample)));
    //gogogo(day11_2(LOAD(11)));
}